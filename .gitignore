# ── 设计稿与逻辑基础稿件（不公开）────────────────────────
# 这四份是这个项目的全部依据，代码是照着它们写的。
# 仓库里只放实现，不放稿子。
# 白名单之外的 md 一律不进仓库。只放 README，
# 设计稿、技术设计、蓝图、时间轴统统留在本地。
*.md
!README.md
设计稿/
草稿/

# ── 打包产物 ────────────────────────────────────────
build/
dist/
*.spec
*.exe

# ── 运行时产生的 ────────────────────────────────────
saves/
*.db
*.sqlite3
selftest.txt

# ── Python ─────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# ── 编辑器 / 系统 ───────────────────────────────────
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
